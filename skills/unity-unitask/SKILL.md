---\nname: unity-unitask\ndescription: "UniTask library for Unity: zero-allocation async/await, PlayerLoop integration, UniTaskTracker, struct-based UniTask vs Task. Installation and patterns for using UniTask correctly in Unity projects."\nrisk: safe\nsource: community\ncategory: game-development\ndate_added: "2026-03-04"\n---\n\n# UniTask\n\nZero-allocation async/await for Unity. Struct-based UniTask replaces Task with no GC overhead and full Unity PlayerLoop integration.\n\n## Use this skill when\n\n- Setting up UniTask in a new Unity project\n- Replacing Task with UniTask for performance\n- Debugging async leaks with UniTaskTracker\n- Awaiting Unity AsyncOperations (LoadSceneAsync, UnityWebRequest, Addressables)\n\n## Instructions\n\n### Installation\n\n````\nopenupm add com.cysharp.unitask\n````\n\n### UniTask vs Task\n\n| Feature | Task | UniTask |\n|---------|------|---------|\n| Allocation | Heap per call | Zero (struct) |\n| Thread | ThreadPool | Unity main thread |\n| WebGL support | No | Yes |\n| Unity ops | Manual marshal | Native PlayerLoop |\n\n### Awaiting Unity Operations\n\n```csharp\n// Scene loading\nawait SceneManager.LoadSceneAsync("Game").ToUniTask();\n\n// UnityWebRequest\nusing var req = UnityWebRequest.Get(url);\nawait req.SendWebRequest().WithCancellation(ct);\nstring json = req.downloadHandler.text;\n\n// Addressables\nvar prefab = await Addressables.LoadAssetAsync<GameObject>("Enemy").ToUniTask();\n```\n\n### VContainer Integration (IAsyncStartable)\n\n```csharp\npublic class GameInitializer : IAsyncStartable\n{\n    private readonly IDataService _data;\n    public GameInitializer(IDataService data) => _data = data;\n\n    public async UniTask StartAsync(CancellationToken ct)\n    {\n        await _data.LoadAsync(ct);\n    }\n}\n\n// In LifetimeScope:\n// builder.RegisterEntryPoint<GameInitializer>();\n```\n\n### UniTaskTracker\n\n````\nWindow > UniTask Tracker\n\nShows all in-flight UniTask operations with stack traces.\nUse to detect leaked async operations that never complete.\n````\n\n### PlayerLoop Timing\n\n```csharp\nawait UniTask.Yield();\nawait UniTask.Yield(PlayerLoopTiming.PreLateUpdate);\nawait UniTask.WaitForEndOfFrame(this, ct);\nawait UniTask.Yield(PlayerLoopTiming.FixedUpdate);\nawait UniTask.DelayFrame(5, cancellationToken: ct);\n```\n\n### Callback to UniTask\n\n```csharp\nvar tcs = new UniTaskCompletionSource<bool>();\nbutton.onClick.AddListener(() => tcs.TrySetResult(true));\nbool confirmed = await tcs.Task;\n```